<style>
    :root { --blue-fr: #002395; --red-fr: #ED2939; --gold: #D4AF37; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; background: #f8f9fa; }
    .header-banner { background: white; border-bottom: 4px solid var(--blue-fr); padding: 20px; text-align: center; position: relative; }
    .logo-central { width: 150px; border: 5px solid transparent; border-image: linear-gradient(to right, blue, white, red) 1; }
    .nav-bar { display: flex; justify-content: center; gap: 20px; background: var(--blue-fr); padding: 10px; }
    .nav-bar a { color: white; text-decoration: none; font-weight: bold; text-transform: uppercase; }
    
    .rupture-card { background: white; border-radius: 15px; padding: 25px; margin: 20px 0; box-shadow: 0 10px 30px rgba(0,0,0,0.1); border-left: 10px solid var(--red-fr); }
    .gain-tag { color: #28a745; font-weight: bold; font-size: 1.2em; }
    .perte-tag { color: #dc3545; font-weight: bold; }
    
    .footer-vision { background: #222; color: white; padding: 40px; margin-top: 50px; border-top: 5px solid var(--gold); }
    .btn-rejoindre { background: var(--gold); color: black; padding: 15px 30px; border-radius: 50px; font-weight: bold; text-decoration: none; display: inline-block; transition: transform 0.3s; }
    .btn-rejoindre:hover { transform: scale(1.1); background: white; }
    
    .future-flash { background: #e9ecef; padding: 10px; border-radius: 5px; font-style: italic; border-left: 5px solid var(--blue-fr); }
</style>
